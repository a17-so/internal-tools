# Slideshow Maker Assets

Static format image packs used for slideshow post generation workflows.

## Structure

- `assets/formats/<format-name>/` contains PNG slides grouped by format theme.
- `assets/app_images/` is reserved for app-specific image sets.

This folder currently contains curated template assets only.
